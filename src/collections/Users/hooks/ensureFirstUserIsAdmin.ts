// Legacy note: the old `ensureFirstUserIsAdmin` hook is no longer used after the split into
// `admins` and `customers`. First-admin creation is now guarded by an explicit bootstrap script.
export {}
