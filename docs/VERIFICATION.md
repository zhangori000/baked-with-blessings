# Verification branch

Preview-only branch to exercise the checkout + cart fixes with Stripe test cards
before trusting them on production. Mirrors `main`; any fixes found during
verification land here first.
