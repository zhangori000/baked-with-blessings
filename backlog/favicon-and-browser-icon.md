# Favicon And Browser Icon

We need to replace the browser tab icon with a real Baked With Blessings favicon instead of the current default-looking asset.

Scope:

- Add proper favicon assets in sizes needed by modern browsers.
- Wire them into the app metadata/layout so Chrome and other browsers pick them up consistently.
- Prefer a simplified logo mark that stays legible at very small sizes.

Notes:

- Check both light and dark browser tab backgrounds.
- Verify desktop Chrome first, then mobile home-screen/icon behavior if we support it later.
