# SEO Foundations

We need a first-pass SEO setup across the site.

Scope:

- Add solid page metadata: titles, descriptions, Open Graph, and Twitter cards.
- Review canonical URLs and indexing behavior for key routes.
- Make sure structured content pages can expose useful metadata from CMS content where appropriate.
- Check sitemap/robots coverage and confirm important public pages are discoverable.

Notes:

- Prioritize home, menu/shop, cookie detail pages, and core landing pages.
- Keep the implementation compatible with Next.js metadata APIs where possible.
